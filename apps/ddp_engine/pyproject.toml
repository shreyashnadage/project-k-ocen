[project]
name = "ddp_engine"
authors = [
    { name = "Project K", email = "dev@projectk.example" }
]
description = "Trust Graph entry, AA data ingestion, thin proprietary-scoring adapter (Phase 3, spec §7)"
requires-python = ">=3.10"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]
