[project]
name = "portal_gateway"
authors = [
    { name = "Project K", email = "dev@projectk.example" }
]
description = "BFF layer — role-aware, cross-doctype composed API responses per frontend (Phase 4, spec §3.5)"
requires-python = ">=3.10"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]
