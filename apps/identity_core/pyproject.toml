[project]
name = "identity_core"
authors = [
    { name = "Project K", email = "dev@projectk.example" }
]
description = "Auth source of truth: Users, Roles, Role Profiles, tenant registry, org hierarchy"
requires-python = ">=3.10"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]
