[project]
name = "crm_extensions"
authors = [
    { name = "Project K", email = "dev@projectk.example" }
]
description = "Anchor relationship pipeline + Vendor lead pipeline, extending Frappe CRM without modifying its core"
requires-python = ">=3.10"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]
